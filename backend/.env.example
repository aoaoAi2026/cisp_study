# CISP 后端环境变量
# 复制此文件为 .env 并修改配置：
#   cp .env.example .env

# 后端监听端口
# 重要：必须与前端 vite.config.ts 中的代理目标端口一致（默认 3003）
# 如果不配置此变量，后端会回退到 3002，导致前端 API 请求失败
PORT=3003

# JWT 签名密钥
# 生产环境请替换为一个强随机字符串（至少 32 字符）
# 可以使用 openssl rand -hex 32 生成
JWT_SECRET=replace-with-a-strong-random-secret
