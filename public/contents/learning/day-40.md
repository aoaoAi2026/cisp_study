# PKI 体系架构：证书管理与密钥生命周期

> **📘 文档定位**：CISP 考试密码学域重点 | 难度：⭐⭐⭐ | 预计阅读：22 分钟
>
> PKI 不仅是一个技术体系，更是一套组织管理制度。本章深入 PKI 的内部运作——证书策略(CP)、认证实践声明(CPS)、密钥生命周期管理、证书跨境互认等——帮你建立 PKI 不仅是"签发证书"的系统化认知。

---

## 导航目录

1. [PKI 的政策体系](#一pki-的政策体系)
2. [密钥生命周期管理](#二密钥生命周期管理)
3. [HSM 硬件安全模块](#三hsm-硬件安全模块)
4. [PKI 部署模式](#四pki-部署模式)
5. [证书透明度 (CT)](#五证书透明度-ct)
6. [CISP 考试要点速查](#六cisp-考试要点速查)

---

## 一、PKI 的政策体系

### CP 与 CPS

```
CP (Certificate Policy) 证书策略
├── 描述证书的适用场景和要求
├── 回答：这个证书可以用来做什么？
└── 例如：该CA签发的证书仅用于企业内网认证

CPS (Certification Practice Statement) 认证实践声明
├── CA 如何执行其职责的操作细节
├── 回答：CA怎么验证身份？怎么保护私钥？
└── 例如：身份验证方式、密钥存储方式、审计频率

CP = 做什么 (What)
CPS = 怎么做 (How)

考试要点：CP 定义规则，CPS 描述实施
```

### PKI 的安全控制

```
CA 的安全保障：

物理安全
├── CA 服务器在安全的机房
├── 多重门禁、24×7 监控
└── 双人控制进入

操作安全
├── 密钥生成仪式 (Key Ceremony)
│   └── 多人见证 + 录像 + 审计 → 生成 Root CA 密钥
├── 双人控制 (2-person control)
│   └── 关键操作需两人同时授权
├── 职责分离
│   └── 系统管理员 ≠ 安全管理员 ≠ 审计管理员
└── 审计日志 → 不可删除

技术安全
├── HSM 存储 CA 私钥
├── 离线 Root CA (不联网)
└── 证书透明度日志
```

---

## 二、密钥生命周期管理

```
密钥生命周期：

生成 (Generation)
├── 在安全的密码设备中生成
├── 使用强随机数发生器 (TRNG)
└── 双人见证 (Root CA 密钥)

分发 (Distribution)
├── 公钥：通过证书传递
├── 私钥：绝不分发，仅存HSM
└── 对称密钥：安全信道 + 密钥加密密钥 (KEK)

存储 (Storage)
├── HSM 存储（CA密钥和重要私钥）
├── 软件存储（普通用户私钥，加密保护）
└── 绝不明文存储私钥

使用 (Usage)
├── 每个密钥只用于一个目的
│   ├── 签名密钥 ≠ 加密密钥
│   └── Root CA 密钥 ≠ 签发证书密钥(Sub CA)
└── 密钥使用时间窗口控制

轮換 (Rotation)
├── CA 密钥定期更换
├── 旧密钥仍可用验签，但不再签发新证书
└── 平滑过渡（新旧证书并存一段时间）

归档 (Archival)
├── 历史密钥归档保存
├── 用于验证历史签名和解密历史数据
└── 安全归档存储

销毁 (Destruction)
├── 密钥不再需要 → 安全销毁
├── HSM 自毁机制
└── 加密擦除 (Crypto Erase)：只销毁密钥材料，数据仍在
```

---

## 三、HSM 硬件安全模块

```
HSM (Hardware Security Module) =

专用硬件设备，用于安全生成、存储和使用密钥

HSM 的核心功能：
├── 密钥在 HSM 内部生成 → 永不离设备
├── 所有密码操作在 HSM 内部完成
├── 防篡改外壳 → 物理攻击 → 自动销毁密钥
├── FIPS 140-2 Level 3/4 认证
└── PKCS#11 API → 标准密码接口

HSM 的 FIPS 140-2 安全等级：

Level 1：基本安全（标准PC+软件）
Level 2：防篡改涂层 + 基于角色的认证
Level 3：防篡改响应（自动清零）+ 基于身份的认证
Level 4：完整物理防护 (温度/电压/辐射检测)

CA 私钥保护 → FIPS 140-2 Level 3 或更高
```

---

## 四、PKI 部署模式

### 企业 PKI vs 公共 PKI

```
企业 PKI (Private PKI / Enterprise PKI)
├── 组织自己搭建的 PKI
├── 自己当 Root CA
├── 签发的证书仅内部有效
├── 例：Microsoft AD CS (Active Directory Certificate Services)
├── 用于：内部VPN/WiFi认证、代码签名、内部系统加密
└── 需要在内网客户端预装企业Root CA证书

公共 PKI (Public PKI / Web PKI)
├── 由公共 CA 提供（DigiCert, Let's Encrypt 等）
├── Root CA 预装在浏览器/操作系统中
├── 签发的证书在互联网上被广泛信任
└── 用于：HTTPS网站、公开 API

托管 PKI (Managed PKI / PKI as a Service)
├── 云端PKI服务
├── 无需自建CA基础设施
└── 例：AWS Private CA, Azure Key Vault
```

### Let's Encrypt 的革新

```
Let's Encrypt =
免费、自动化、开放的公共 CA

核心创新：
├── ACME 协议 → 自动化证书申请和续签
├── 域验证 → 只需证明你控制该域名
├── 90天短有效期 → 促进自动化
├── 完全免费 → HTTPS 普及的关键推手
└── 透明度 → 所有颁发的证书都在 CT 日志中

影响：HTTPS 使用率从 2015年的 40% → 85%+
```

---

## 五、证书透明度 (CT)

```
Certificate Transparency (CT) 证书透明度 =

解决 CA 被攻破或 rogue CA 签发假证书的问题

工作原理：
1. CA 签发证书时 → 必须将证书提交到 CT 日志
2. CT 日志 → 仅追加，不可修改，公开可查
3. 浏览器要求 TLS 证书附带 SCT (Signed Certificate Timestamp)
   证明该证书已在 CT 日志中记录

几个著名的 CA 事故：
├── 2011 DigiNotar CA 被黑 → 签发 *.google.com 假证书
│   → 伊朗大量用户被中间人攻击
│   → DigiNotar 倒闭
├── 2012 Trustwave 签发中间 CA → 被用于企业 SSL 解密
├── 2015 联想 Superfish → 预装自签根证书做 HTTPS 中间人
└── → CT 的推出就在这些事件之后
```

---

## 六、CISP 考试要点速查

| 概念 | 要点 |
|------|------|
| CP (证书策略) | 做什么 |
| CPS (认证实践声明) | 怎么做 |
| HSM | 硬件安全模块→保护CA私钥 |
| 密钥轮換 | 定期更换，旧钥仍可验签 |
| CT | 证书透明度→防止CA签发假证书 |
| ACME | 自动化证书管理协议 (Let's Encrypt) |

### 密钥使用分离原则

```
不同目的 → 不同密钥对！

签名密钥对 ≠ 加密密钥对

为什么？
├── 签名密钥：如丢失，可吊销 → 新签
├── 加密密钥：如丢失，历史密文永久无法解密
└── 加密密钥需要备份 (Key Escrow)！
   签名密钥绝不应该备份！

这也是为什么：
├── CA 的签名密钥在 HSM 中
├── 而加密密钥需要安全备份
└── 两者分开管理！
```

### 常见考试陷阱

1. ~~PKI 只是技术系统~~ → PKI是技术+策略+法律+人员的综合体系
2. ~~所有密钥都需要备份~~ → 签名密钥不应备份（防不可否认性被破坏）
3. ~~HSM 是可选的~~ → CA 私钥必须用 HSM 保护
4. ~~证书和密钥是一回事~~ → 证书包含公钥，私钥单独存储

---

> 💡 **本章小结**：PKI 体系架构的核心是理解它不仅是技术还有管理维度。CP/CPS 的区别、密钥生命周期、HSM 的作用、CT 的背景——这些都是 CISP 考试中 PKI 域的考点，但难度不如前几章的对称/非对称加密高。
