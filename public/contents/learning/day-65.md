# Day 65：威胁建模

> **所属周**：Week 10 — 安全工程 · **主题**：STRIDE、攻击树与安全设计

---

## 📑 目录

1. [威胁建模概述](#一威胁建模概述)
2. [STRIDE模型详解](#二stride模型详解)
3. [攻击树分析](#三攻击树分析)
4. [数据流图(DFD)](#四数据流图dfd)
5. [威胁建模流程](#五威胁建模流程)
6. [CISP考试速查](#cisp考试速查)
7. [自检清单](#自检清单)

---

## 一、威胁建模概述

### 1.1 威胁建模价值

```
威胁建模：在设计阶段系统地识别和应对安全威胁

核心问题：
  "如果我是攻击者，我会怎么攻击这个系统？"

价值：
  ✅ 设计阶段发现安全问题（修复成本最低）
  ✅ 系统化发现所有潜在威胁
  ✅ 优先级排序
  ✅ 指导安全测试
```

### 1.2 方法论对比

| 方法论 | 特点 | 适用 |
|--------|------|------|
| STRIDE | 微软，6类威胁分类 | 通用软件系统 |
| PASTA | 风险为中心 | 企业风险管理 |
| Trike | 开源，需求驱动 | 安全审计 |
| VAST | 可扩展、敏捷 | DevOps环境 |
| Attack Tree | 攻击路径可视化 | 复杂系统 |

---

## 二、STRIDE模型详解

### 2.1 六类威胁

```
S → Spoofing (欺骗/冒充)
  "我是管理员！"(伪造身份)
  防御：强认证(多因素、证书)

T → Tampering (篡改)
  "我已经修改了订单金额"
  防御：完整性校验、数字签名、WORM

R → Repudiation (抵赖/否认)
  "这个交易不是我做的！"
  防御：审计日志、数字签名、区块链存证

I → Information Disclosure (信息泄露)
  "我看到了用户密码"
  防御：加密、访问控制、数据脱敏

D → Denial of Service (拒绝服务)
  "服务器已经无法响应了"
  防御：冗余、限流、CDN/WAF

E → Elevation of Privilege (权限提升)
  "我现在是管理员了"
  防御：最小权限、沙箱、安全编码
```

### 2.2 STRIDE对应安全属性

```
CIA匹配：
  S(欺骗) → 认证(Authentication)
  T(篡改) → 完整性(Integrity)
  R(抵赖) → 不可否认性(Non-Repudiation)
  I(泄露) → 机密性(Confidentiality)
  D(拒绝服务) → 可用性(Availability)
  E(提权) → 授权(Authorization)
```

---

## 三、攻击树分析

### 3.1 攻击树结构

```
攻击目标：盗取用户数据库
              (OR)
         ┌─────┴─────┐
         │           │
    Web应用攻击    物理入侵
       (OR)        (AND)
   ┌────┴────┐   ┌──┴──┐
   │         │   │     │
  SQL注入   XSS  撬门  破解门禁
  (OR)            (成本:高)
  ┌──┴──┐
  │     │
Union 盲注
(成本:中)

叶子节点标注：
  · 成本/难度 (Cost/Difficulty)
  · 是否需要认证
  · 是否需要物理接触
```

### 3.2 攻击树价值

```
分析攻击树可以：
  → 找到最便宜的攻击路径(最小化成本)
  → 找到最短的攻击路径
  → 确定关键防御节点
  → 量化安全投资回报
```

---

## 四、数据流图(DFD)

```
DFD元素与威胁映射表：

┌──────────┬──────────┬──────────────────────┐
│ DFD元素   │   符号    │   STRIDE威胁检查     │
├──────────┼──────────┼──────────────────────┤
│ 外部实体   │  □□□     │ S(欺骗)            │
│          │  用户     │                      │
├──────────┼──────────┼──────────────────────┤
│ 进程      │   ○      │ S,T,R,I,D,E(全六项)  │
├──────────┼──────────┼──────────────────────┤
│ 数据存储   │  ═══     │ T(篡改),I(泄露)     │
├──────────┼──────────┼──────────────────────┤
│ 数据流     │   →→     │ T,I,D               │
├──────────┼──────────┼──────────────────────┤
│ 信任边界   │ ─ ─ ─   │ 跨越边界=高风险      │
└──────────┴──────────┴──────────────────────┘

每画一条数据流，检查STRIDE六项！
```

---

## 五、威胁建模流程

```
Microsoft威胁建模四步法：

Step 1：绘制数据流图(DFD)
  → 外部实体、进程、数据存储、数据流、信任边界

Step 2：识别威胁(STRIDE-by-DFD)
  → 对每个元素检查STRIDE六项

Step 3：响应威胁
  → 标准缓解方案 → 特定实现措施

Step 4：验证
  → 每个威胁是否被充分缓解？
  → 残留风险可接受吗？
```

---

## 六、CISP考试速查

| 考点 | 记忆要点 |
|------|---------|
| STRIDE六字母 | "Spoofing, Tampering, Repudiation, Info Disclosure, DoS, Elevation" |
| STRIDE vs CIA | "STRIDE六类→对应认证/完整性/不可否认/机密性/可用性/授权" |
| 信任边界 | "跨信任边界的数据流=高风险区域" |
| 攻击树 | "OR节点(任一可达到)，AND节点(全部需要)" |

---

## 七、自检清单

- [ ] STRIDE六个字母分别代表什么？
- [ ] STRIDE每个威胁对应的防御方向？
- [ ] 攻击树中OR节点和AND节点的含义？
- [ ] DFD五个核心元素与STRIDE的映射？
- [ ] Microsoft威胁建模四步法？

---

> **下一步**：Day 66 安全架构设计——纵深防御、安全模式与框架。
