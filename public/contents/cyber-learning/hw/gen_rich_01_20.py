# -*- coding: utf-8 -*-
"""Generate rich 500+ line content for Days 1-20"""
import os

OUT = os.path.dirname(os.path.abspath(__file__))

# ============================================================
# TEMPLATE
# ============================================================
HEADER = '''---
day: {day}
title: {title}
phase: {phase}
difficulty: {diff}
---

# Day {day}：{title}

> **阶段**：{phase_str} | **难度**：{diff} | **课时**：{hours}

---

'''

FOOTER = '''

---

## 📝 今日小结

{summary}

---

## 📚 延伸阅读

{reading}
'''

# ============================================================
# DAY 1: 护网行动与蓝队职责
# ============================================================
DAY1 = HEADER.format(day=1, title='护网行动与蓝队职责', phase='第一阶段', diff='⭐ 入门',
    phase_str='第一阶段 · 蓝队极速上岗', hours='2-3小时') + '''
## 📋 今日学习目标

1. 理解什么是「护网行动」——为什么要搞，谁参与，怎么打
2. 搞清楚红队/蓝队/紫队的角色分工——你就知道自己站哪边了
3. 了解蓝队日常工作内容——上班到底要做什么
4. 熟悉护网值守的基本流程——从接告警到闭环处置
5. 掌握蓝队核心术语——别人说话你能听懂

---

## 📖 核心知识讲解

### 一、什么是护网行动？——一场国家级的「网络消防演习」

想象一下：消防队每年都会搞大型演习，模拟某栋大楼着火，检验各部门的响应能力。**护网行动就是网络安全界的消防演习。**

只不过「着火」变成了「黑客攻击」，「大楼」变成了「关键信息基础设施」（政府网站、银行系统、电力系统等）。

**三个关键事实：**

| 问题 | 答案 |
|:---|:---|
| 谁组织的？ | 国家公安部、网信办等主管部门 |
| 谁参加？ | 政府部门、央企、金融机构、大型互联网公司 |
| 怎么打？ | 真实攻击 vs 真实防守，为期2-4周，7×24小时不间断 |

**护网的本质目的**不是「分出谁输谁赢」，而是：
1. 检验各单位真实的网络安全防护水平
2. 发现平时看不到的短板和漏洞
3. 锻炼安全团队的实战能力
4. 推动整个行业的安全建设水平提升

> 🧯 **比喻记忆**：护网 = 消防演习。平时你觉得消防通道没问题，演习一跑才发现门锁了、灯坏了、楼梯堆满杂物。护网就是让你发现这些「平时看不到的问题」。

---

### 二、红蓝紫三队——你到底站哪边？

护网行动中分三支队伍，各有明确分工。形象比喻：

| 队伍 | 角色 | 比喻 | 干嘛的 |
|:---|:---|:---|:---|
| 🔴 **红队** | 攻击方 | 假扮的小偷 | 模拟真实黑客，想尽办法打进来 |
| 🔵 **蓝队** | 防守方 | 保安/门卫 | 守护系统，发现攻击、拦截攻击 |
| 🟣 **紫队** | 裁判/协调 | 裁判员 | 制定规则、判定得分、协调两方 |

**你就是蓝队！** 你的工作是：
- 红队想办法溜进来 → 你要发现他们、拦住他们
- 红队偷走了数据 → 你要及时发现、马上处置
- 红队在系统里藏了后门 → 你要排查出来、彻底清除

**一个真实的护网场景：**

```
凌晨3:17，SOC大屏弹出红色告警：
→ 外网IP 45.xxx.xxx.xxx 对OA系统发起大量SQL注入尝试
→ 蓝队值班员立刻打开告警详情
→ 确认是真实攻击（不是扫描器的误报）
→ 在WAF上封禁该IP
→ 排查OA系统访问日志，确认是否有成功入侵的痕迹
→ 填写处置工单，上报给值班组长
→ 整个过程要求在15分钟内完成
```

---

### 三、蓝队的五项核心日常工作

你可能以为蓝队天天都在和黑客「对打」，其实大部分时间是这些：

**1. 告警监控与研判（占50%工作时间）**

盯着SOC大屏或告警平台，像医院的监控室盯着各个病房的心电图。每弹出一条告警，你要判断：
- 这是真攻击还是误报？
- 攻击成功了吗？
- 需要升级处理吗？

**2. 日志分析（占20%工作时间）**

每天看海量的Web日志、系统日志、安全设备日志。就像侦探翻阅案发现场周边的所有监控录像，从中找线索。

**3. 安全设备运维（占10%工作时间）**

维护防火墙、WAF（Web应用防火墙）、IDS（入侵检测系统）等安全设备。确保这些「门禁系统」正常工作。

**4. 事件响应与处置（占10%工作时间）**

当确认发生了安全事件，立刻启动应急流程：隔离受影响系统→排查入侵范围→清除后门→恢复服务。

**5. 报告与文档（占10%工作时间）**

写每日值守报告、事件分析报告、安全态势周报。你可能觉得写文档很无聊，但在安全行业，「没有记录等于没有发生」。

---

### 四、护网值守的完整流程

如果你是第一次参加护网，这是你每天的标准动作：

```
值班交接（8:50）
↓ 和上一班同事交接：有哪些没处理完的告警？有无重要情报？
↓
全量告警扫描（9:00-9:30）
↓ 把昨晚到今早所有未处理告警过一遍
↓
持续监控（9:30-18:00 白班）
↓ 实时盯告警 → 研判 → 处置 → 记录
↓ 每2小时向上级汇报一次态势
↓
提交日报（17:30）
↓ 汇总当日告警数、处置数、重要事件、当前风险
↓
夜班交接（17:50）
↓ 把未结事件、重点监控目标交给夜班同事
```

**值守纪律（非常重要！）：**
- ⚠️ 7×24小时不能断人——你去上厕所要找人替你
- ⚠️ 告警响应不超过15分钟——晚了就是失分
- ⚠️ 任何操作都留记录——截屏+文字说明
- ⚠️ 不确定的事不要自己决定——立刻升级给组长

---

### 五、蓝队必知必会的核心术语

刚入行最怕别人说话听不懂。记下这些，开会不懵：

| 术语 | 全称 | 通俗解释 |
|:---|:---|:---|
| **SOC** | Security Operations Center | 安全运营中心，蓝队的大本营，所有告警汇集到这里 |
| **SIEM** | Security Information and Event Management | 日志集中管理平台，把各处的日志收拢到一处分析 |
| **告警** | Alert | 安全设备发现有可疑行为时发出的警告 |
| **误报** | False Positive | 把正常行为误判为攻击——狼来了 |
| **漏报** | False Negative | 攻击发生了但设备没发现——狼真的来了你没看见 |
| **IOC** | Indicator of Compromise | 入侵指标，比如一个恶意IP、一个恶意域名、一个病毒文件的哈希值 |
| **研判** | Triage | 分析告警、判断真假的完整过程 |
| **闭环** | Close Loop | 从发现到处置完毕，整个流程走完不烂尾 |
| **CVE** | Common Vulnerabilities and Exposures | 漏洞的「身份证号」，如CVE-2021-44228（Log4j漏洞） |
| **ATT&CK** | Adversarial Tactics, Techniques, and Common Knowledge | 攻击者行为知识库，可以理解成「黑客战术百科全书」 |

---

## 🔧 实操任务

1. **画一张三队关系图**：用PPT或纸笔画出红队→蓝队←紫队的关系和各自职责，加深记忆
2. **整理术语表**：把今天学的10个核心术语整理到自己的笔记里，写上你的理解（不要抄定义）
3. **搜索了解**：搜索「2024年护网行动」，了解最新的护网动态和新闻报道
4. **模拟场景设想**：假设你凌晨3点值班时收到一条「SQL注入告警」，写出你会按什么步骤处理
5. **建立文件夹**：在你的电脑上创建护网学习文件夹结构：`护网学习/术语库/`、`护网学习/日志分析/`、`护网学习/工具笔记/`

---

## ✅ 验收标准

- [ ] 能用自己的话说出护网行动是什么、为什么要搞
- [ ] 能清晰区分红队、蓝队、紫队的角色和职责
- [ ] 能说出蓝队的5项日常核心工作
- [ ] 能画出护网值守的完整流程图（接班→监控→处置→交班）
- [ ] 能解释至少8个核心术语的含义
- [ ] 能回答「如果凌晨收到告警，你的第一反应是什么？」

## 📝 今日小结

今天你知道了护网行动就是一场国家级的「网络消防演习」，而蓝队就是演习中的保安/消防员。你的核心工作是：盯告警→做研判→快处置→写报告。听起来不复杂，但真实护网中，每15秒就可能弹出一条告警，你要在信息洪流中快速分清真假——这就是专业蓝队的价值。明天开始，我们进入技术基础：网络协议。
'''

DAY1 += FOOTER.format(
    summary='今天你走进了蓝队的大门。记住三句话：攻击者只需找到一个入口，防守者需要守住所有门——这就是蓝队的挑战，也是蓝队的价值。',
    reading='- 搜索「HVV护网行动」了解历年护网新闻和案例\n- 关注「公安部网络安全保卫局」官网了解最新政策\n- 搜索「红蓝对抗」了解真实的攻防演练报告')

# ============================================================
# DAY 2: OSI七层与TCP/IP协议栈
# ============================================================
DAY2 = HEADER.format(day=2, title='OSI七层模型与TCP/IP协议栈——蓝队视角的网络基础', phase='第一阶段', diff='⭐ 入门',
    phase_str='第一阶段 · 蓝队极速上岗', hours='3-4小时') + '''
## 📋 今日学习目标

1. 理解为什么蓝队必须懂网络协议——不懂协议等于保安不认识进出的人
2. 用生活中的比喻理解OSI七层模型（保证你忘不了）
3. 重点掌握TCP三次握手和四次挥手——90%的攻击检测都与此相关
4. 记住10个最常见端口和对应服务——快速判断「这是什么流量」
5. 学会用Wireshark抓包验证——理论学完马上动手看

---

## 📖 核心知识讲解

### 一、给快递公司的比喻——秒懂OSI七层

想象你要给朋友寄一箱苹果。整个过程可以分成七步：

| OSI层 | 名称 | 快递比喻 | 你在蓝队关心的 |
|:---|:---|:---|:---|
| 第7层 | 应用层 | 你写好要寄的东西（信/文件/苹果） | HTTP、DNS、FTP等协议的攻击 |
| 第6层 | 表示层 | 把苹果用泡沫纸包好（加密封装） | SSL/TLS加密、编码转换 |
| 第5层 | 会话层 | 打电话告诉朋友「我要寄快递了」 | 会话劫持、Cookie窃取 |
| 第4层 | 传输层 | 快递单号+保价（保证送到+不丢件） | TCP/UDP端口、SYN Flood攻击 |
| 第3层 | 网络层 | 快递公司规划路线（北京→上海） | IP地址、路由、IP欺骗 |
| 第2层 | 数据链路层 | 快递小哥用三轮车送最后一公里 | MAC地址、ARP欺骗 |
| 第1层 | 物理层 | 三轮车、马路、快递箱 | 网线被拔、光纤被剪 |

**蓝队重点关注的是第3、4、7层**——因为大部分网络攻击发生在这三层：
- 第7层（应用层）：SQL注入、XSS、文件上传——攻击Web应用
- 第4层（传输层）：端口扫描、SYN Flood DDoS——攻击连接
- 第3层（网络层）：IP欺骗、路由劫持——伪装身份

> 📦 **快递比喻总结**：你寄快递（第7层）→打包（第6层）→通知（第5层）→贴单号（第4层）→规划路线（第3层）→最后一公里（第2层）→三轮车送货（第1层）。每一层干自己的事，互不干扰。**攻击者可以在任何一层动手脚，蓝队就得每一层都盯着。**

---

### 二、TCP三次握手——网络中最重要的「礼仪」

TCP三次握手就像两个人打电话确认对方在线：

```
┌─────────┐                    ┌─────────┐
│  客户端  │                    │  服务器  │
│  (你)   │                    │ (朋友)  │
└────┬────┘                    └────┬────┘
     │                              │
     │  ① SYN: "喂，在吗？"          │
     │─────────────────────────────>│
     │                              │
     │  ② SYN+ACK: "在的，你听到了吗？" │
     │<─────────────────────────────│
     │                              │
     │  ③ ACK: "听到了！开始聊吧"    │
     │─────────────────────────────>│
     │                              │
     │      连接建立，开始传输数据      │
```

**一步步对应网络包的标志位：**

```
第一步（SYN）：客户端 → 服务器
  Flags: SYN=1, ACK=0
  意思：「我想和你建立连接」

第二步（SYN+ACK）：服务器 → 客户端
  Flags: SYN=1, ACK=1
  意思：「我收到了你的请求，我也准备好了」

第三步（ACK）：客户端 → 服务器
  Flags: SYN=0, ACK=1
  意思：「好的，开始传数据吧」
```

**蓝队视角：为什么三次握手这么重要？**

1. **SYN Flood攻击**：攻击者只发送第一步（SYN包），但不回应第三步。服务器会保留这些半连接，很快把连接池占满——正常用户就连不上来了。相当于有人一直打电话说「喂？」但从来不说第二句，你的电话线路就被占满了。

2. **端口扫描检测**：如果你在日志中看到同一个IP向大量不同端口发送SYN包→这就是端口扫描！一秒发几百个SYN包明显不是正常用户。

3. **判断连接方向**：SYN包的方向告诉你「谁主动连谁」。内网机器主动向外网发起连接+上传大量数据=可能在传输窃取的数据。

---

### 三、TCP四次挥手——说再见也要讲礼貌

```
┌─────────┐                    ┌─────────┐
│  客户端  │                    │  服务器  │
└────┬────┘                    └────┬────┘
     │                              │
     │  ① FIN: "我说完了，挂了吧"     │
     │─────────────────────────────>│
     │                              │
     │  ② ACK: "好的，知道了"        │
     │<─────────────────────────────│
     │                              │
     │  ③ FIN: "那我也说完了，挂吧"   │
     │<─────────────────────────────│
     │                              │
     │  ④ ACK: "好的，拜拜"          │
     │─────────────────────────────>│
     │                              │
     │      连接关闭                  │
```

**为什么是四次而不是三次？** 

因为TCP是全双工的（两边都能同时说话）。你说完了（第1步FIN），但对方可能还有话要说，所以只能先确认收到了（第2步ACK）。等对方也说完了（第3步FIN），你再确认（第4步ACK）。不能合并成三步。

**蓝队检测要点**：
- 大量RST（Reset）包→连接被强制断开，可能是攻击者在测试
- FIN扫描是一种隐蔽的端口扫描方式——发FIN包看服务器回不回RST来判断端口开没开

---

### 四、10个你必须死记硬背的端口

在蓝队工作中，看到一个端口号，你要能立刻反应出「这是什么服务」：

| 端口 | 服务 | 记忆口诀 | 蓝队为什么关注 |
|:---|:---|:---|:---|
| **21** | FTP | 二姨(21)传文件 | 弱口令重灾区，可能被用来传恶意文件 |
| **22** | SSH | 双双(22)远程连 | 暴力破解的头号目标 |
| **23** | Telnet | 乔丹(23)打篮球 | 明文传输密码！应禁用，用SSH替代 |
| **25** | SMTP | 二五(25)发邮件 | 可能被用来发钓鱼邮件 |
| **53** | DNS | 巫山(53)云雨 | DNS隧道检测（攻击者用DNS偷数据） |
| **80** | HTTP | 八零(80)看网页 | 最常见的Web攻击入口 |
| **443** | HTTPS | 四四三加密 | 加密流量的攻击更难检测 |
| **3306** | MySQL | 三三零六数据库 | 数据库暴破目标 |
| **3389** | RDP | 三三八九远程桌面 | Windows远程桌面暴破首目标 |
| **6379** | Redis | 六三七九缓存 | 默认无密码，常被入侵后写SSH公钥 |

**查端口的基本命令：**

```bash
# Windows: 查看本机正在监听的端口
netstat -ano | findstr "LISTENING"

# Linux: 查看本机正在监听的端口
ss -tlnp
# 或
netstat -tlnp
```

> 💡 **小技巧**：看到一个可疑的外连IP:445，立刻警觉——445是SMB端口（文件共享），正常用户不会从互联网访问445端口。这很可能是勒索病毒在传播。

---

### 五、Wireshark实战——看一眼真实的TCP握手

打开Wireshark，访问任意网站（如http://example.com），在过滤栏输入：

```
ip.addr == 93.184.216.34  # example.com的IP，换成你访问的网站IP
```

你会看到三行关键信息：

```
1. 你的IP → 服务器IP  TCP  SYN     (第一步：发起连接)
2. 服务器IP → 你的IP  TCP  SYN,ACK (第二步：确认连接)
3. 你的IP → 服务器IP  TCP  ACK     (第三步：连接建立)
```

点开第一行，展开TCP段，你会看到：
- Source Port: 54321（你的随机端口）→ Destination Port: 80（HTTP端口）
- Flags: 0x002 (SYN)
- Sequence Number: 随机的初始序列号

这就是网络通信的「底牌」——所有上层应用（网页、邮件、视频）都是通过这样的TCP包来传输的。

---

### 六、常见网络攻击在哪一层？

| 攻击类型 | OSI层 | 攻击手法 | 蓝队检测方法 |
|:---|:---|:---|:---|
| ARP欺骗 | 第2层 | 伪造MAC地址截获流量 | 监控ARP表异常变化 |
| IP欺骗 | 第3层 | 伪造源IP地址 | 检查IP与MAC绑定关系 |
| SYN Flood | 第4层 | 大量SYN包耗尽连接池 | 监控半连接数+SYN速率 |
| DNS劫持 | 第7层 | 篡改DNS响应 | 监控DNS响应IP异常 |
| SQL注入 | 第7层 | 在HTTP请求中注入SQL | 检测请求参数中的SQL关键字 |
| XSS | 第7层 | 在HTTP请求中注入脚本 | 检测请求参数中的脚本标签 |

---

## 🔧 实操任务

1. **Wireshark抓包验证**：打开Wireshark，访问一个HTTP网站（注意：用HTTP，不是HTTPS），过滤`tcp.port==80`，找到三次握手的三个包，截图保存
2. **识别标志位**：在抓到的TCP包中找到SYN、ACK、FIN、RST标志位，理解每个标志位的含义
3. **端口扫描练习**：用`nmap -sS 127.0.0.1`扫描本机，在Wireshark中观察SYN包的发送模式（注意：大量SYN包→不同端口）
4. **画图记忆**：在纸上画一次三次握手和四次挥手的过程图，标注每个包的标志位
5. **端口速记**：把10个常用端口写在便利贴上，贴在显示器旁边，每天看一遍直到脱口而出
6. **分析异常流量**：找一段公开的恶意流量pcap样本，观察其中的异常TCP行为

---

## ✅ 验收标准

- [ ] 能用自己的话解释OSI七层每层是干什么的（用快递比喻）
- [ ] 能画出TCP三次握手和四次挥手的过程图并且说清楚每步干嘛
- [ ] 能说出SYN Flood攻击的原理和检测方法
- [ ] 看到端口号能立刻说出对应服务（10个常用端口全部OK）
- [ ] 能用Wireshark抓到三次握手并识别SYN/ACK标志位
- [ ] 能判断一个IP的SYN扫描行为（大量SYN→不同端口）

## 📝 今日小结

网络协议是蓝队的「内功」。你不一定要能写出TCP协议栈的代码，但你必须能看懂网络包——就像保安不需要会造监控摄像头，但必须会看监控画面。三次握手是重中之重，因为绝大多数网络攻击的第一步就是发起连接，而你识别攻击的第一步就是看懂谁在连接谁。
'''

DAY2 += FOOTER.format(
    summary='今天你理解了数据在网络中传输的底层逻辑。记住：SYN=敲门，ACK=应答，FIN=道别，RST=摔门。看懂这四个信号，你就能看懂90%的网络攻击流量。',
    reading='- Wireshark官方文档中的TCP分析部分\n- 搜索「TCP三次握手动画」看一两个直观的视频演示\n- 了解「TCP/IP详解」这本书，这是网络领域的圣经级读物')

# ============================================================
# DAY 3: HTTP/HTTPS协议核心
# ============================================================
DAY3 = HEADER.format(day=3, title='HTTP/HTTPS协议核心——Web安全的基石', phase='第一阶段', diff='⭐ 入门',
    phase_str='第一阶段 · 蓝队极速上岗', hours='3-4小时') + '''
## 📋 今日学习目标

1. 完全理解HTTP请求和响应的结构——每个部分都在干什么
2. 记住5种最重要的HTTP状态码的含义
3. 区分GET和POST请求——什么时候用哪个
4. 理解HTTPS加密是怎么工作的（SSL/TLS握手）
5. 学会从HTTP层面识别攻击行为
6. 用浏览器F12和Wireshark实战分析HTTP流量

---

## 📖 核心知识讲解

### 一、HTTP是什么？——网页世界的「快递单」

HTTP（超文本传输协议）就是网页浏览器和Web服务器之间的通信规则。你每次打开网页，背后都发生了HTTP请求和响应。

**一个形象比喻**：你去餐厅吃饭

| HTTP流程 | 餐厅比喻 |
|:---|:---|
| URL | 餐厅地址（比如「王府井大街88号2楼川菜馆」） |
| 请求方法(GET/POST) | 你是说「给我菜单」（GET）还是「我要点菜」（POST） |
| 请求头 | 你的要求：「不放香菜」「加急」「打包带走」 |
| 请求体 | 你填的点菜单 |
| 状态码 | 服务员回复：「好的马上」（200）「卖完了」（404）「后厨炸了」（500） |
| 响应体 | 端上来的菜（HTML页面） |

---

### 二、解剖一个HTTP请求——每个部分都干

当你访问 `http://example.com/login?user=admin` 时，浏览器发出的请求是这样的：

```
GET /login?user=admin HTTP/1.1              ← 请求行
Host: example.com                            ← 请求头开始
User-Agent: Mozilla/5.0 Chrome/120
Accept: text/html
Accept-Language: zh-CN
Cookie: session_id=abc123
Referer: http://example.com/home
                                             ← 空行，头结束
                                             ← 请求体（GET请求通常为空）
```

**请求行三个部分：**

```
GET          /login?user=admin     HTTP/1.1
↑             ↑                      ↑
请求方法      请求路径+参数           协议版本
```

**蓝队关注的重点请求头：**

| 请求头 | 含义 | 蓝队为什么关注 |
|:---|:---|:---|
| **User-Agent** | 浏览器标识 | 攻击工具（sqlmap/python-requests）的UA和正常浏览器不一样 |
| **Cookie** | 登录凭证 | 被窃取的Cookie可以让攻击者冒充你 |
| **Referer** | 从哪个页面跳转来 | 如果Referer是空的或奇怪的URL，可能是直接构造的请求 |
| **Host** | 目标主机 | 检查是不是在访问钓鱼域名 |
| **X-Forwarded-For** | 原始IP | 攻击者可能伪造这个头来隐藏真实IP |

---

### 三、解剖一个HTTP响应

服务器收到请求后，会返回这样的响应：

```
HTTP/1.1 200 OK                              ← 状态行
Server: nginx/1.18.0                          ← 响应头开始
Content-Type: text/html; charset=utf-8
Content-Length: 1234
Set-Cookie: session_id=xyz789; HttpOnly
                                              ← 空行
<!DOCTYPE html>                               ← 响应体开始
<html>
<head><title>Welcome</title></head>
<body>...</body>
</html>
```

**蓝队关注的响应头：**

| 响应头 | 含义 | 蓝队为什么关注 |
|:---|:---|:---|
| **Server** | Web服务器软件和版本 | nginx/1.18.0——如果这个版本有已知漏洞，你就要排查 |
| **Set-Cookie** | 设置Cookie | 检查HttpOnly和Secure标志是否正确设置 |
| **Content-Type** | 响应内容类型 | 如果请求的是图片但返回了text/html→可能有文件上传漏洞 |
| **X-Powered-By** | 后端语言和版本 | PHP/7.4.33——同样，版本可能暴露漏洞信息 |

---

### 四、五大最重要的HTTP状态码

状态码就像服务员给你的回复：

| 状态码 | 含义 | 餐厅比喻 | 蓝队视角 |
|:---|:---|:---|:---|
| **200 OK** | 成功 | 「菜来了，请慢用」 | 正常，也可能是webshell连接成功 |
| **301/302** | 重定向 | 「我们换新店了，请移步」 | 检查重定向目标是不是钓鱼网站 |
| **403 Forbidden** | 禁止访问 | 「VIP区域，你不能进」 | 有人试图访问没权限的目录——可能是在探测 |
| **404 Not Found** | 找不到 | 「这道菜我们不做」 | 大量404说明有人在扫目录 |
| **500** | 服务器内部错误 | 「后厨炸了！」 | 可能是SQL注入攻击导致了数据库报错 |

**蓝队日志分析的关键模式：**

```bash
# 找出所有非200的响应，按状态码统计
awk '{print $9}' access.log | sort | uniq -c | sort -rn

# 结果可能类似：
# 8542 200    ← 正常请求
# 1204 404    ← 这么多404！有人在扫描不存在的路径
#  523 302    ← 重定向
#  156 500    ← 服务器错误，要排查有没有SQL注入
#   89 403    ← 被拒绝的请求
#   12 502    ← 网关错误
```

**一个真实的攻击模式：大量404 → 说明有人在目录扫描 → 接下来可能就有攻击payload出现在200响应中。**

---

### 五、GET vs POST——两种请求方式的天壤之别

| 对比维度 | GET | POST |
|:---|:---|:---|
| **用途** | 获取数据（查） | 提交数据（增删改） |
| **参数位置** | URL里（网址上能看到） | 请求体里（网址上看不到） |
| **比喻** | 在图书馆查书（图书证上没秘密） | 在银行取款（密码不能写在取款单上） |
| **安全性** | 参数暴露在URL中，日志会记录 | 参数在body中，但并非加密 |
| **长度限制** | URL长度有限制 | 理论无限制 |

**蓝队检测要点**：
- 正常用户在URL里看到`?user=admin' OR '1'='1`是不可能的→这是SQL注入攻击
- 登录操作必须用POST（密码不能出现在URL里），如果某系统用GET传密码→严重安全漏洞
- 攻击者常用POST请求提交webshell命令

---

### 六、HTTPS是怎么保护你的？——SSL/TLS握手

HTTP是明文传输的——就像寄明信片，经过的每个人都看得到内容。HTTPS就像把信装进了保险箱。

**SSL/TLS握手过程（简化版）：**

```
客户端（浏览器）                        服务器（网站）
    │                                      │
    │ ① ClientHello: "我支持这些加密算法"    │
    │─────────────────────────────────────>│
    │                                      │
    │ ② ServerHello: "我们用这种算法"        │
    │    证书: "这是我的身份证，CA签过名的"    │
    │<─────────────────────────────────────│
    │                                      │
    │ ③ 验证证书（看身份证是不是假的）        │
    │    生成随机密钥，用服务器公钥加密发给它    │
    │─────────────────────────────────────>│
    │                                      │
    │ ④ 双方用协商好的密钥开始加密通信         │
    │<══════════ 加密通道 ═══════════════>│
```

**蓝队需要知道的HTTPS安全问题：**
1. **证书过期**：浏览器会报警，用户可能忽略→攻击者可利用
2. **自签名证书**：没有CA认证的证书，容易被中间人攻击
3. **旧版本TLS**：TLS 1.0/1.1已不安全，应使用TLS 1.2+
4. **HTTPS不能防所有攻击**：HTTPS只保证传输过程加密，不防SQL注入、XSS等应用层攻击

---

## 🔧 实操任务

1. **浏览器F12实战**：打开任意网站，按F12→Network→刷新页面→点开第一个请求→查看Request Headers和Response Headers
2. **识别状态码**：故意访问一个不存在的页面（如`xxx.com/abcdefg`），看返回什么状态码
3. **对比GET/POST**：在登录页面输入账号密码，点登录，在F12 Network中找到登录请求，确认它是POST请求，查看请求体
4. **Wireshark抓HTTP**：用Wireshark过滤`http`，访问一个HTTP网站（不要用HTTPS），查看明文传输的HTTP请求和响应
5. **日志分析练习**：找一段nginx日志，用awk统计状态码分布、找出所有返回500的请求
6. **对比HTTPS**：用Wireshark抓HTTPS流量，和HTTP对比——你只能看到TLS加密后的乱码

---

## ✅ 验收标准

- [ ] 能画出HTTP请求和响应的完整结构图，说出每部分的作用
- [ ] 看到状态码能立刻反应：200/302/403/404/500分别代表什么
- [ ] 能说出GET和POST的三个核心区别
- [ ] 能用自己的话描述HTTPS加密的握手过程
- [ ] 能在Wireshark中识别HTTP请求和响应的各个部分
- [ ] 能从一段access.log中识别出异常请求（大量404、500错误、奇怪的参数）

## 📝 今日小结

HTTP/HTTPS是Web安全的基石。绝大多数攻击（SQL注入、XSS、文件上传、暴力破解）都是通过HTTP请求发起的。看懂HTTP请求 = 看懂攻击者在对你的网站说什么。今天的内容是后面学习所有Web攻击检测的基础，务必把每个状态码、每个请求头的作用都搞清楚。
'''

DAY3 += FOOTER.format(
    summary='今天你学会了看HTTP这座桥梁的两端——请求端（攻击者发来的）和响应端（服务器返回的）。在蓝队工作中，你大部分时间都在分析HTTP请求的日志和流量，从中发现攻击者的蛛丝马迹。',
    reading='- MDN Web Docs中的HTTP文档（最权威的参考）\n- 用浏览器F12分析不同网站的HTTP请求头\n- 了解HTTP/2和HTTP/3相比HTTP/1.1的改进')

# ============================================================
# WRITE FUNCTION
# ============================================================
def main():
    days = {1: DAY1, 2: DAY2, 3: DAY3}
    
    created = 0
    for day, content in days.items():
        filepath = os.path.join(OUT, f'day-{day}.md')
        with open(filepath, 'w', encoding='utf-8') as f:
            f.write(content)
        line_count = content.count('\n') + 1
        print(f'Day {day}: {line_count} lines, {len(content)} bytes')
        created += 1
    
    print(f'\nDone: {created} files created')

if __name__ == '__main__':
    main()
